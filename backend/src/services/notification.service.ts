// Placeholder for push notifications (OneSignal/Firebase)
export async function sendPushNotification(title: string, body: string, _studentId?: string) {
  // TODO: integrate OneSignal or Firebase Cloud Messaging
  console.log(`[Notification] ${title}: ${body}`)
}
